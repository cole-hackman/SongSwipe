# Keep line endings correct across OSes so the setup/start scripts always run.
# macOS shell scripts must be LF (CRLF breaks the interpreter); Windows .bat must be CRLF.
*.command text eol=lf
*.sh      text eol=lf
*.bat     text eol=crlf
